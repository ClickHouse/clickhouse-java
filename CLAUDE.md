# Claude Instructions

Follow `AGENTS.md` for repository-wide guidance.

When asked to review code, pull requests, commits, or diffs, follow `docs/ai-review.md`.

When review work touches `client-v2` or `jdbc-v2`, use `docs/features.md` as a compatibility contract.
